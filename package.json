{
  "name": "mongoose-public-id",
  "version": "1.0.1",
  "description": "Generates a public ID for your models which you can expose in your API (similar to Stripe).",
  "main": "./src/index.js",
  "repository": "https://github.com/sandrinodimattia/mongoose-public-id",
  "author": "Sandrino Di Mattia",
  "license": "MIT",
  "scripts": {
    "test": "eslint --ignore-path .gitignore . && nyc ava",
    "test:watch": "ava --watch",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  },
  "dependencies": {
    "hashids": "^1.1.1"
  },
  "devDependencies": {
    "ava": "^0.19.1",
    "coveralls": "^2.13.1",
    "eslint": "^3.19.0",
    "eslint-config-airbnb-base": "^11.1.3",
    "eslint-plugin-import": "^2.2.0",
    "nyc": "^10.3.2"
  }
}
